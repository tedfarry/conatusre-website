---
description: Git remote and push configuration for this project
globs: "*"
---

This project uses the `tedfarry` GitHub account.
Remote: `git@github.com:tedfarry/conatusre-website.git` (SSH, push enabled)

After completing work on a task branch, always commit and push:
```bash
git add -A
git commit -m "Description of changes"
git push -u origin <branch-name>
```
